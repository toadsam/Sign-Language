from __future__ import annotations

from pathlib import Path

from docx import Document

from build_sign_language_tech_doc import (
    FILL_OK,
    FILL_WARN,
    OUT as TECH_OUT,
    add_bullet,
    add_callout,
    add_code,
    add_flow,
    add_h1,
    add_h2,
    add_h3,
    add_numbered_list,
    add_para,
    add_table,
    add_title,
    configure_document,
)


ROOT = Path(__file__).resolve().parents[1]
OUT = ROOT / "docs" / "sign-language-concepts-deep-dive-ko.docx"


def build_doc():
    doc = Document()
    configure_document(doc)

    add_title(
        doc,
        "수어지교 개념 심화 문서",
        "문서 안에 등장하는 핵심 개념을 초보자 기준으로 매우 자세하게 풀어 쓴 별도 참고서",
    )
    add_callout(
        doc,
        "이 문서의 성격",
        "이 문서는 기능 설명 문서가 아니다. 프런트엔드, 백엔드, API, 인증, 데이터 저장, 번역 파이프라인 등 기존 문서에 등장한 개념 자체를 깊게 이해하기 위한 참고서다. 실제 프로젝트를 볼 때 모르는 단어가 나오면 이 문서에서 개념을 다시 확인하는 용도로 읽으면 된다.",
        FILL_OK,
    )
    add_para(
        doc,
        "설명 방식은 최대한 기초부터 시작한다. 먼저 아주 넓은 개념을 설명하고, 그다음 더 작은 개념으로 내려가며, 마지막에는 이 프로젝트 안에서 그 개념이 실제로 어떻게 나타나는지 연결한다."
    )

    add_h1(doc, "1. 이 문서를 읽는 방법")
    add_para(
        doc,
        "처음부터 끝까지 순서대로 읽어도 되지만, 실제로는 모르는 개념이 생길 때 필요한 장만 골라서 읽어도 된다. 예를 들어 API가 헷갈리면 API 장만 보고, JWT가 헷갈리면 인증 장만 봐도 된다."
    )
    add_numbered_list(
        doc,
        [
            "개발을 거의 처음 접한다면 2장, 3장, 4장을 먼저 읽는다.",
            "화면 코드가 궁금하다면 5장을 먼저 읽는다.",
            "서버 코드가 궁금하다면 6장을 먼저 읽는다.",
            "로그인과 보안이 헷갈리면 7장을 읽는다.",
            "Firestore나 Storage가 헷갈리면 8장과 9장을 읽는다.",
            "번역 로직에 나오는 형태소, 토큰, 정규화가 헷갈리면 10장을 읽는다.",
            "전체 요청 흐름을 한 번에 보고 싶다면 11장을 읽는다.",
        ],
    )
    add_callout(
        doc,
        "중요한 학습 습관",
        "처음에는 모든 단어를 완벽히 외우려고 하지 않는 편이 좋다. 큰 흐름을 먼저 잡고, 그 다음 반복해서 보면서 세부 용어를 익히는 편이 훨씬 빠르다.",
    )

    add_h1(doc, "2. 디지털 서비스라는 것은 무엇인가")
    add_para(
        doc,
        "앱이나 웹 서비스는 겉으로는 한 덩어리처럼 보이지만 실제로는 여러 층으로 이루어져 있다. 사용자가 보는 화면이 있고, 그 화면 뒤에서 일을 처리하는 서버가 있고, 그 서버가 읽고 쓰는 데이터 저장소가 있다. 여기에 로그인 시스템이나 외부 API가 추가되면 더 복합적인 구조가 된다."
    )
    add_table(
        doc,
        ["구성 요소", "역할", "비유"],
        [
            ("사용자", "서비스를 사용하는 사람", "가게 손님"),
            ("프런트엔드", "사용자가 직접 보는 화면", "가게의 카운터와 메뉴판"),
            ("백엔드", "요청을 처리하는 로직", "주문을 받아 음식을 만드는 주방"),
            ("데이터베이스", "지속적으로 저장되는 정보", "가게의 장부와 재고 목록"),
            ("파일 저장소", "영상, 이미지 같은 큰 파일 보관", "창고"),
            ("외부 API", "바깥 서비스에서 빌려오는 기능", "다른 업체의 납품 또는 외주"),
        ],
        [1.4, 2.6, 2.5],
    )
    add_para(
        doc,
        "이 프로젝트도 같은 구조다. 프런트엔드는 사용자가 번역기와 퀴즈 화면을 보는 곳이고, 백엔드는 문장을 처리하고 문제를 채점하는 곳이며, Firestore는 사용자 기록과 문제 데이터를 저장하고, Firebase Storage는 수어 영상을 저장한다."
    )

    add_h1(doc, "3. 클라이언트와 서버라는 개념")
    add_h2(doc, "3.1 클라이언트란 무엇인가")
    add_para(
        doc,
        "클라이언트는 서비스를 사용하는 쪽 프로그램이다. 보통 브라우저나 모바일 앱이 여기에 해당한다. 사용자의 입력을 받고, 화면을 보여 주고, 서버에게 필요한 일을 요청한다. 이 프로젝트에서는 Expo/React Native 앱이 클라이언트다."
    )
    add_h2(doc, "3.2 서버란 무엇인가")
    add_para(
        doc,
        "서버는 클라이언트의 요청을 받아 처리하는 프로그램이다. 혼자 화면을 그리지는 않지만, 데이터 계산, 저장, 검증, 인증, 외부 연동 같은 중요한 일을 담당한다. 이 프로젝트에서는 Spring Boot 백엔드가 서버다."
    )
    add_h2(doc, "3.3 왜 둘을 나누는가")
    add_para(
        doc,
        "초보자는 종종 '그냥 앱 안에서 다 하면 안 되나'라고 생각한다. 하지만 화면 코드 안에서 데이터 저장, 인증 검증, 외부 키 관리, 공용 비즈니스 로직까지 전부 처리하면 보안과 유지보수가 매우 나빠진다. 그래서 사용자가 직접 만지는 부분과, 믿을 수 있는 중앙 처리 부분을 분리한다."
    )
    add_bullet(doc, "프런트엔드는 자주 바뀌는 UI와 상호작용에 강하다.")
    add_bullet(doc, "백엔드는 공통 규칙과 데이터 보호에 강하다.")
    add_bullet(doc, "서버 한 곳에서 처리하면 여러 사용자에게 같은 규칙을 적용하기 쉽다.")
    add_bullet(doc, "비밀 키와 민감한 로직을 앱 안에 숨기지 않고 서버에 둘 수 있다.")
    add_h2(doc, "3.4 요청과 응답")
    add_para(
        doc,
        "클라이언트와 서버는 보통 요청과 응답이라는 방식으로 대화한다. 클라이언트가 '이 문장을 번역해 달라'고 요청하면 서버가 작업을 하고 결과를 응답으로 돌려준다. 이 한 번의 왕복이 서비스의 기본 단위다."
    )
    add_flow(
        doc,
        "기본 요청 구조",
        [
            "클라이언트가 요청을 만든다",
            "  ↓",
            "서버가 요청을 받는다",
            "  ↓",
            "서버가 검증, 계산, 저장, 조회를 수행한다",
            "  ↓",
            "서버가 응답을 만든다",
            "  ↓",
            "클라이언트가 결과를 받아 화면을 바꾼다",
        ],
    )

    add_h1(doc, "4. API, HTTP, URL, JSON을 깊게 이해하기")
    add_h2(doc, "4.1 API란 무엇인가")
    add_para(
        doc,
        "API는 서로 다른 프로그램이 대화하는 약속이다. 사람이 말할 때 언어와 문법이 필요하듯, 프로그램도 어떤 주소로 어떤 형식의 데이터를 보내고 어떤 형식으로 받는지 정해 둔 규칙이 필요하다. 그 규칙이 API다."
    )
    add_para(
        doc,
        "예를 들어 '/translate'라는 API가 있다고 하면, 프런트엔드는 여기에 문장을 보내고, 백엔드는 번역 결과를 JSON으로 돌려준다는 약속을 갖는다. 프런트엔드는 서버 내부 구현을 몰라도 API 계약만 알면 사용할 수 있다."
    )
    add_h2(doc, "4.2 HTTP란 무엇인가")
    add_para(
        doc,
        "HTTP는 웹과 앱에서 가장 널리 쓰는 통신 방식이다. 브라우저가 페이지를 받을 때도, 모바일 앱이 서버에서 데이터를 받을 때도, 대부분 HTTP를 쓴다. API는 대개 HTTP 위에서 동작한다."
    )
    add_table(
        doc,
        ["메서드", "주로 언제 쓰나", "쉽게 이해하기"],
        [
            ("GET", "데이터를 읽을 때", "꺼내 보기"),
            ("POST", "새 요청을 만들거나 계산을 맡길 때", "새로 보내기"),
            ("PATCH", "기존 데이터 일부를 바꿀 때", "부분 수정"),
            ("DELETE", "데이터를 지우거나 숨길 때", "삭제 요청"),
        ],
        [1.0, 2.1, 3.4],
    )
    add_h2(doc, "4.3 URL과 경로")
    add_para(
        doc,
        "URL은 서버 자원을 가리키는 주소다. 주소에는 프로토콜, 호스트, 포트, 경로 같은 요소가 들어간다. 예를 들어 'http://localhost:8080/translate'에서는 http가 통신 방식, localhost가 서버 위치, 8080이 포트, /translate가 경로다."
    )
    add_bullet(doc, "프로토콜은 어떻게 통신할지 말한다. 여기서는 http 또는 https가 흔하다.")
    add_bullet(doc, "호스트는 어느 컴퓨터에 요청할지 말한다.")
    add_bullet(doc, "포트는 그 컴퓨터 안의 어떤 프로그램과 대화할지 말한다.")
    add_bullet(doc, "경로는 그 프로그램 안에서 어떤 기능을 호출할지 말한다.")
    add_h2(doc, "4.4 JSON이 중요한 이유")
    add_para(
        doc,
        "JSON은 사람이 읽기 쉬우면서 프로그램도 다루기 쉬운 데이터 형식이다. 프런트엔드 JavaScript와 백엔드 Java 모두 비교적 간단하게 JSON을 읽고 쓸 수 있어서 API 응답에 매우 많이 사용된다."
    )
    add_code(
        doc,
        """{
  "text": "오늘 학교에 가요"
}""",
    )
    add_para(
        doc,
        "이 예시는 번역 요청에 보낼 수 있는 간단한 JSON이다. 키는 text이고 값은 사용자가 입력한 문장이다. 서버는 이 JSON을 읽어 번역 작업을 시작한다."
    )
    add_h2(doc, "4.5 API 계약이라는 생각")
    add_para(
        doc,
        "API를 설계할 때 가장 중요한 생각 중 하나는 계약이다. 계약이란 입력과 출력의 약속이다. 예를 들어 입력 JSON에 text가 있어야 한다면 프런트엔드는 그 약속을 지켜야 하고, 서버가 normalizedTokens와 clips를 준다고 약속했다면 프런트엔드는 그 값을 기대하고 화면을 구성할 수 있다."
    )
    add_callout(
        doc,
        "왜 계약이 중요할까",
        "프런트엔드와 백엔드가 동시에 바뀌는 프로젝트에서는 누가 무엇을 기대하는지 문서화되어 있지 않으면 금방 깨진다. API는 코드이기도 하지만 동시에 팀 간 약속이기도 하다.",
    )

    add_h1(doc, "5. 프런트엔드 개념을 매우 자세하게 이해하기")
    add_h2(doc, "5.1 프런트엔드는 단순한 디자인이 아니다")
    add_para(
        doc,
        "초보자는 프런트엔드를 그저 예쁘게 만드는 일로 오해하기 쉽다. 실제로는 훨씬 더 많은 일을 한다. 화면을 그리는 일뿐 아니라, 입력을 받고, 상태를 관리하고, API를 호출하고, 응답을 기다리는 동안 로딩을 보여 주고, 실패하면 에러를 표시하고, 성공하면 결과를 재배치하는 논리까지 포함된다."
    )
    add_h2(doc, "5.2 컴포넌트란 무엇인가")
    add_para(
        doc,
        "컴포넌트는 화면을 조각으로 나누는 단위다. 버튼 하나도 컴포넌트가 될 수 있고, 번역 화면 전체도 하나의 컴포넌트가 될 수 있다. 화면을 조각으로 나누면 재사용과 유지보수가 쉬워진다."
    )
    add_bullet(doc, "작은 컴포넌트는 버튼, 입력창, 카드처럼 재사용하기 쉽다.")
    add_bullet(doc, "큰 컴포넌트는 화면 전체 흐름을 관리한다.")
    add_bullet(doc, "컴포넌트는 종종 다른 컴포넌트를 포함한다.")
    add_h2(doc, "5.3 상태(State)란 무엇인가")
    add_para(
        doc,
        "상태는 화면이나 프로그램이 현재 기억하는 값이다. 번역 입력창의 텍스트, 로딩 중인지 여부, 현재 문제 번호, 로그인한 사용자 정보가 모두 상태다. React에서는 상태가 바뀌면 화면이 다시 그려진다."
    )
    add_table(
        doc,
        ["상태 예시", "무슨 뜻인가", "왜 필요한가"],
        [
            ("text", "사용자가 입력창에 쓴 문장", "번역 요청을 보낼 때 필요하다."),
            ("isLoading", "현재 요청 진행 중인지", "버튼 비활성화와 로딩 UI에 필요하다."),
            ("result", "번역 응답 결과", "결과 화면을 그릴 때 필요하다."),
            ("questions", "퀴즈 문제 배열", "문제를 화면에 보여 주기 위해 필요하다."),
            ("user", "로그인한 사용자 정보", "개인화된 화면과 API 호출에 필요하다."),
        ],
        [1.2, 2.3, 2.9],
    )
    add_h2(doc, "5.4 리렌더링이란 무엇인가")
    add_para(
        doc,
        "리렌더링은 상태가 바뀌었을 때 화면을 다시 계산하는 과정이다. 사용자가 버튼을 누르고 isLoading이 true가 되면, React는 '이제 화면이 달라져야 하겠구나'라고 판단하고 다시 그린다. 그래서 코드에서는 값만 바꾸었는데 화면이 자동으로 바뀌는 것처럼 보인다."
    )
    add_h2(doc, "5.5 Props란 무엇인가")
    add_para(
        doc,
        "Props는 부모 컴포넌트가 자식 컴포넌트에 주는 값이다. 예를 들어 부모가 버튼 컴포넌트에게 '이 글자를 표시해라' 또는 '눌렸을 때 이 함수를 실행해라'라고 넘겨 주는 값이 props다. props는 바깥에서 주는 입력값이라고 생각하면 된다."
    )
    add_h2(doc, "5.6 이벤트와 핸들러")
    add_para(
        doc,
        "사용자 행동은 이벤트를 만든다. 버튼 클릭, 입력창 변경, 화면 진입 같은 것이 이벤트다. 핸들러는 그 이벤트가 일어났을 때 실행되는 함수다. 예를 들어 번역 버튼 핸들러는 현재 text 상태를 읽어 API를 호출한다."
    )
    add_h2(doc, "5.7 Effect란 무엇인가")
    add_para(
        doc,
        "Effect는 화면을 그리는 것 자체가 아니라, 그 밖의 부수 작업을 실행하는 장치다. API 요청, 타이머 등록, 로컬 저장소 읽기 같은 일이 effect에 자주 들어간다. 초보자는 effect를 '화면 외부 세계와 연결되는 작업'으로 이해하면 쉽다."
    )
    add_h2(doc, "5.8 Context란 무엇인가")
    add_para(
        doc,
        "Context는 여러 화면이나 컴포넌트가 공통으로 써야 하는 상태를 공유하는 방법이다. 이 프로젝트에서는 인증 상태가 대표적이다. user와 accessToken을 모든 화면에 props로 계속 넘기기보다는 auth context에서 공통으로 꺼내 쓰게 하면 구조가 간단해진다."
    )
    add_h2(doc, "5.9 Router란 무엇인가")
    add_para(
        doc,
        "Router는 지금 어떤 화면을 보여 줄지 정하는 장치다. URL 또는 앱 내부 경로에 따라 login, signup, home, translator 같은 서로 다른 화면을 보여 준다. Expo Router는 파일 구조를 기준으로 경로를 관리한다."
    )
    add_h2(doc, "5.10 로딩, 에러, 빈 상태")
    add_para(
        doc,
        "초보자는 성공한 화면만 만들고 끝내기 쉽지만, 실제 서비스에서는 세 가지 상태를 항상 같이 생각해야 한다. 로딩 상태는 응답을 기다리는 동안의 화면이고, 에러 상태는 실패했을 때의 화면이며, 빈 상태는 데이터가 아직 없을 때의 화면이다."
    )
    add_bullet(doc, "로딩 상태가 없으면 사용자는 앱이 멈췄다고 느낀다.")
    add_bullet(doc, "에러 상태가 없으면 실패 원인을 모른다.")
    add_bullet(doc, "빈 상태가 없으면 데이터가 없는 것과 로딩 중인 것을 구분하기 어렵다.")
    add_h2(doc, "5.11 로컬 저장소")
    add_para(
        doc,
        "프런트엔드는 앱을 껐다 켜도 유지되어야 하는 값을 로컬 저장소에 보관할 수 있다. 이 프로젝트에서는 모바일에서는 SecureStore, 웹에서는 localStorage를 쓴다. accessToken과 user를 저장해 두면 앱을 다시 열었을 때 로그인 상태를 복원할 수 있다."
    )
    add_callout(
        doc,
        "상태와 저장소는 다르다",
        "상태는 현재 실행 중인 메모리 값이고, 저장소는 앱을 껐다 켜도 남는 값이다. 둘은 비슷해 보이지만 역할이 다르다.",
    )

    add_h1(doc, "6. 백엔드 개념을 매우 자세하게 이해하기")
    add_h2(doc, "6.1 백엔드는 왜 필요한가")
    add_para(
        doc,
        "백엔드는 공통 규칙을 한곳에 모으고, 여러 사용자가 같은 기준을 따르게 하며, 민감한 처리와 저장을 안전하게 맡는 계층이다. 예를 들어 퀴즈 정답 판정을 앱 안에서만 하면 사용자가 코드를 바꿔 버릴 수 있다. 서버가 판정하면 기준이 중앙에 남는다."
    )
    add_h2(doc, "6.2 Controller")
    add_para(
        doc,
        "Controller는 HTTP 요청이 처음 도착하는 입구다. 어떤 URL과 어떤 메서드로 요청이 왔는지 보고, 적절한 서비스로 넘기고, 최종 응답을 만든다. 보통 복잡한 비즈니스 규칙은 Controller에 오래 두지 않는 편이 좋다."
    )
    add_h2(doc, "6.3 Service")
    add_para(
        doc,
        "Service는 실제 비즈니스 로직이 들어가는 계층이다. 번역 서비스, 퀴즈 서비스, 사용자 서비스 같은 이름이 붙는 이유는 각각 맡는 업무가 다르기 때문이다. 코드가 커질수록 Controller보다 Service에 더 중요한 로직이 쌓인다."
    )
    add_h2(doc, "6.4 Config")
    add_para(
        doc,
        "Config는 애플리케이션 운영 규칙을 넣는 곳이다. 예를 들어 Firebase 연결, 보안 정책, CORS 허용 범위, 정적 리소스 처리 같은 것은 특정 기능보다 애플리케이션 전반에 영향을 주므로 설정 클래스로 분리하는 경우가 많다."
    )
    add_h2(doc, "6.5 Bean과 Dependency Injection")
    add_para(
        doc,
        "Spring에서는 필요한 객체를 직접 new 하는 대신, 프레임워크가 만들어 관리하는 Bean을 주입받아 쓴다. 이 방식을 Dependency Injection이라고 부른다. 초보자 기준으로는 '내가 전선 하나하나 연결하지 않아도 스프링이 연결해 준다' 정도로 이해해도 충분하다."
    )
    add_bullet(doc, "직접 객체를 만들지 않으면 테스트와 교체가 쉬워진다.")
    add_bullet(doc, "구성 요소 사이의 의존성이 더 명확해진다.")
    add_bullet(doc, "스프링이 시작할 때 필요한 것들을 미리 준비할 수 있다.")
    add_h2(doc, "6.6 DTO")
    add_para(
        doc,
        "DTO는 데이터를 운반하기 위한 객체다. API 요청 바디를 Java 객체로 받거나, 응답을 정리해서 JSON으로 내보낼 때 쓴다. DTO를 두면 프런트엔드와 주고받는 데이터 구조가 더 명확해진다."
    )
    add_h2(doc, "6.7 비즈니스 로직")
    add_para(
        doc,
        "비즈니스 로직은 서비스가 실제로 지켜야 하는 규칙이다. 예를 들어 퀴즈 choices는 4개여야 한다, 번역 후보는 사전 hit가 더 높은 쪽을 선택한다, 로그인한 사용자는 본인 기록만 수정해야 한다 같은 것들이 모두 비즈니스 로직이다."
    )
    add_h2(doc, "6.8 검증과 예외")
    add_para(
        doc,
        "요청이 항상 올바르게 들어온다고 가정하면 위험하다. 값이 비어 있을 수도 있고, 잘못된 형식일 수도 있고, 없는 사용자 ID를 보낼 수도 있다. 그래서 서버는 입력을 검증하고, 문제가 있으면 적절한 예외나 에러 응답을 돌려줘야 한다."
    )
    add_h2(doc, "6.9 서버 상태와 무상태")
    add_para(
        doc,
        "많은 HTTP API는 무상태에 가깝게 설계된다. 즉 요청 하나가 처리될 때 필요한 정보는 그 요청 안에 들어 있어야 한다. JWT가 필요한 이유도 여기와 연결된다. 서버가 메모리에 사용자 로그인 상태를 계속 들고 있지 않아도, 요청에 토큰이 오면 그걸 보고 사용자를 확인할 수 있다."
    )

    add_h1(doc, "7. 인증과 보안을 매우 자세하게 이해하기")
    add_h2(doc, "7.1 인증과 인가의 차이")
    add_para(
        doc,
        "인증은 '당신이 누구인가'를 확인하는 과정이고, 인가는 '그 사람이 무엇을 할 수 있는가'를 정하는 과정이다. 둘은 비슷해 보이지만 다르다. 로그인은 인증에 가깝고, 본인 데이터만 수정 가능하게 하는 것은 인가에 가깝다."
    )
    add_h2(doc, "7.2 OAuth와 Google 로그인")
    add_para(
        doc,
        "OAuth는 외부 서비스가 대신 신원 확인을 도와주는 구조다. 사용자는 Google 계정으로 로그인하고, 우리 서비스는 Google이 '이 사용자는 검증되었다'고 알려 준 결과를 활용한다. 이 프로젝트는 Google 로그인 뒤 idToken을 받아 서버로 보낸다."
    )
    add_h2(doc, "7.3 idToken이란 무엇인가")
    add_para(
        doc,
        "idToken은 사용자가 특정 Google 계정으로 로그인했다는 정보를 담은 토큰이다. 백엔드는 이 토큰이 진짜 Google이 발급한 것인지, 그리고 우리 앱을 대상으로 발급된 것인지 확인한다. 이 검증을 통과해야 로그인 흐름이 진행된다."
    )
    add_h2(doc, "7.4 JWT란 무엇인가")
    add_para(
        doc,
        "JWT는 서버가 자체적으로 발급하는 서명된 토큰이다. Google이 로그인 사실을 증명한 뒤, 우리 서버는 '이제 이 사용자를 우리 서비스 안에서 어떻게 식별할지'를 위해 JWT를 만든다. 이후 요청에서 이 토큰을 보내면 서버가 사용자를 알아볼 수 있다."
    )
    add_h2(doc, "7.5 JWT Secret과 만료")
    add_para(
        doc,
        "JWT는 아무나 만들 수 있으면 안 되므로 비밀 키로 서명한다. 그 비밀 키가 JWT_SECRET이다. 또한 토큰은 영원히 유효하면 위험하므로 보통 만료 시간을 둔다. 만료가 지나면 다시 로그인하거나 갱신해야 한다."
    )
    add_h2(doc, "7.6 왜 프런트엔드와 백엔드가 둘 다 인증을 신경 써야 하나")
    add_para(
        doc,
        "프런트엔드는 로그인 화면을 보여 주고 토큰을 저장하고 요청에 붙이는 일을 맡는다. 백엔드는 그 토큰을 검증하고 실제 권한을 판단한다. 둘 중 하나만 잘해도 충분하지 않다. 프런트엔드는 사용자 경험을 만들고, 백엔드는 보안을 보장한다."
    )
    add_h2(doc, "7.7 CORS란 무엇인가")
    add_para(
        doc,
        "CORS는 브라우저가 다른 주소의 서버와 통신할 때 적용하는 보안 규칙이다. 프런트엔드와 백엔드가 서로 다른 호스트나 포트에서 실행되면 브라우저는 '정말 이 통신을 허용해도 되나'를 확인한다. 그래서 서버에서 허용할 origin을 명시해 주어야 한다."
    )
    add_h2(doc, "7.8 CSRF는 왜 언급되는가")
    add_para(
        doc,
        "CSRF는 사용자의 인증 상태를 악용해 의도하지 않은 요청을 보내게 만드는 공격이다. 모든 프로젝트가 똑같이 취약한 것은 아니지만, 인증 구조를 설계할 때 항상 언급되는 대표적인 웹 보안 개념이다."
    )
    add_h2(doc, "7.9 permitAll이 의미하는 것")
    add_para(
        doc,
        "Spring Security에서 permitAll은 해당 경로를 인증 없이도 통과시키겠다는 뜻이다. 개발 초기에는 편하지만, 민감한 사용자 데이터를 다루는 API를 너무 넓게 permitAll로 열어 두면 운영 환경에서 위험할 수 있다."
    )
    add_callout(
        doc,
        "보안에서 자주 하는 실수",
        "프런트엔드에서 로그인 화면을 막았으니 안전하다고 생각하는 경우가 많다. 그러나 진짜 보안은 서버에서 강제되어야 한다. 화면에서 가려도 네트워크 요청은 직접 보낼 수 있기 때문이다.",
        FILL_WARN,
    )

    add_h1(doc, "8. 데이터베이스와 Firestore를 매우 자세하게 이해하기")
    add_h2(doc, "8.1 데이터베이스란 무엇인가")
    add_para(
        doc,
        "데이터베이스는 앱이 기억해야 할 정보를 오래 저장하는 곳이다. 앱을 껐다 켜도, 사용자가 바뀌어도, 서버를 재시작해도 데이터가 남아 있어야 할 때 데이터베이스가 필요하다."
    )
    add_h2(doc, "8.2 관계형 데이터베이스와 문서형 데이터베이스")
    add_para(
        doc,
        "모든 데이터베이스가 같은 모양은 아니다. 전통적인 관계형 데이터베이스는 표와 행과 열 중심으로 생각하고, Firestore 같은 문서형 데이터베이스는 JSON 비슷한 문서 중심으로 생각한다. 이 프로젝트는 Firestore를 쓰므로 문서 단위 사고가 중요하다."
    )
    add_h2(doc, "8.3 Firestore의 핵심 단위")
    add_table(
        doc,
        ["단위", "뜻", "예시"],
        [
            ("Collection", "문서들의 묶음", "users, quiz_items"),
            ("Document", "하나의 데이터 항목", "특정 사용자 문서"),
            ("Field", "문서 안의 값", "name, email, isRegistered"),
            ("Subcollection", "문서 아래의 하위 묶음", "users/{uid}/translator_bookmarks"),
        ],
        [1.25, 2.35, 2.9],
    )
    add_h2(doc, "8.4 왜 Firestore를 쓰는가")
    add_para(
        doc,
        "Firestore는 Firebase 생태계와 잘 맞고, 문서 구조가 직관적이며, 사용자별 데이터나 하위 컬렉션 구조를 빠르게 만들기 편하다. 앱 서비스에서 유연하게 데이터를 저장하고 싶은 경우 자주 선택된다."
    )
    add_h2(doc, "8.5 스키마리스라는 말의 장점과 함정")
    add_para(
        doc,
        "Firestore는 엄격한 테이블 스키마가 없어 자유롭게 필드를 넣기 쉽다. 이것은 초기 개발 속도에는 장점이지만, 시간이 지나면 문서마다 구조가 조금씩 달라져 혼란을 만들 수 있다. 그래서 문서형 데이터베이스도 사실상 스키마 문서화를 해 두는 편이 좋다."
    )
    add_h2(doc, "8.6 사용자 문서와 하위 컬렉션")
    add_para(
        doc,
        "이 프로젝트에서는 users 컬렉션 아래에 사용자 문서가 있고, 그 아래에 번역 북마크나 오답노트 같은 하위 컬렉션이 붙는다. 이 구조는 '사용자별로 묶어서 생각하기'에는 편하지만, 반대로 전체 사용자 데이터를 한 번에 집계할 때는 별도 전략이 필요할 수 있다."
    )
    add_h2(doc, "8.7 Map 형태 필드")
    add_para(
        doc,
        "incorrectQuestionCounts나 dailySolvedCounts처럼 key-value 맵으로 저장되는 필드는 매우 유용하다. 문제 ID별 오답 횟수나 날짜별 풀이 수를 한 문서 안에 압축적으로 담을 수 있다. 다만 너무 커지면 문서 크기와 갱신 전략을 고민해야 한다."
    )
    add_h2(doc, "8.8 읽기와 쓰기 비용 관점")
    add_para(
        doc,
        "Firestore는 문서 단위 읽기와 쓰기라는 사고가 중요하다. 문서를 자주 읽거나 자주 쓰면 비용과 성능이 달라질 수 있다. 그래서 데이터 구조를 정할 때 '이 값을 얼마나 자주 읽고 쓸까'도 생각해야 한다."
    )

    add_h1(doc, "9. 파일 저장소와 Firebase Storage를 매우 자세하게 이해하기")
    add_h2(doc, "9.1 왜 데이터베이스와 파일 저장소를 나누는가")
    add_para(
        doc,
        "사용자 이름이나 점수처럼 구조화된 값은 데이터베이스에 잘 맞지만, 큰 영상 파일 자체를 데이터베이스에 넣는 것은 비효율적이다. 그래서 보통 메타데이터는 데이터베이스에 두고, 실제 파일은 별도 저장소에 둔다."
    )
    add_h2(doc, "9.2 Bucket, Object, Path")
    add_para(
        doc,
        "Storage는 보통 버킷이라는 큰 저장 공간 안에 파일 객체를 경로 기반으로 넣는다. 마치 폴더처럼 보이지만 실제로는 객체 이름에 경로처럼 보이는 문자열이 붙어 있는 구조라고 생각해도 된다."
    )
    add_h2(doc, "9.3 다운로드 URL")
    add_para(
        doc,
        "사용자 앱이 영상을 재생하려면 결국 재생 가능한 URL이 필요하다. 백엔드는 Storage 안의 파일을 찾아서 그 파일을 내려받거나 스트리밍할 수 있는 URL을 만든다. direct URL이 되기도 하고 signed URL이 되기도 한다."
    )
    add_h2(doc, "9.4 Signed URL")
    add_para(
        doc,
        "Signed URL은 일정 시간 동안만 유효한 접근 주소다. 파일을 완전히 공개하지 않으면서도 필요한 동안만 접근을 허용할 수 있다. 백엔드가 권한을 가지고 서명한 뒤 사용자에게 넘겨 주는 구조다."
    )
    add_h2(doc, "9.5 캐시(Cache)")
    add_para(
        doc,
        "Storage에서 같은 파일을 반복해서 찾는 작업은 낭비일 수 있다. 그래서 한번 찾은 결과를 메모리에 저장해 두었다가 다음에는 바로 꺼내 쓰는 캐시가 유용하다. StorageVideoCache가 바로 그런 역할을 한다."
    )
    add_bullet(doc, "캐시는 속도를 올린다.")
    add_bullet(doc, "반복 조회를 줄여 비용과 지연을 줄인다.")
    add_bullet(doc, "하지만 원본이 바뀌었을 때 갱신 전략이 필요하다.")

    add_h1(doc, "10. 언어 처리 개념을 매우 자세하게 이해하기")
    add_h2(doc, "10.1 왜 번역은 단순 문자열 치환이 아닌가")
    add_para(
        doc,
        "사람이 쓰는 문장은 형태가 매우 다양하다. 같은 뜻이라도 표현이 다를 수 있고, 조사와 시제와 부정과 질문 형태가 섞인다. 그래서 번역은 단순히 사전에서 단어 하나씩 찾는 일보다 훨씬 어렵다."
    )
    add_h2(doc, "10.2 토큰(Token)")
    add_para(
        doc,
        "토큰은 문장을 처리하기 위해 잘라 낸 단위다. 보통 단어 비슷한 단위로 생각하면 되지만, 실제로는 어디를 경계로 자를지에 따라 결과가 달라질 수 있다. 번역 파이프라인에서 토큰은 사전과 매칭되는 기본 재료다."
    )
    add_h2(doc, "10.3 형태소(Morpheme)")
    add_para(
        doc,
        "형태소는 의미를 가진 가장 작은 말 단위다. 자연어 처리에서는 단어보다 더 작은 단위까지 분석할 때가 많다. 예를 들어 조사나 어미도 형태소 수준에서 다룰 수 있다."
    )
    add_h2(doc, "10.4 정규화(Normalization)")
    add_para(
        doc,
        "정규화는 입력 표현을 일정한 형태로 정리하는 과정이다. 공백을 정리하고, 활용형을 기본형에 가깝게 만들고, 불필요한 기호를 제거해 사전과 연결하기 쉽게 한다."
    )
    add_h2(doc, "10.5 Lemma와 기본형")
    add_para(
        doc,
        "활용형 단어를 기본형으로 돌리는 생각은 매우 중요하다. 사용자는 '가요', '갔어요', '가는 중'처럼 여러 형태를 입력할 수 있지만, 사전은 '가다'처럼 기본형 중심으로 되어 있을 수 있기 때문이다."
    )
    add_h2(doc, "10.6 문장 단순화")
    add_para(
        doc,
        "문장 단순화는 한국어 문장을 사전 매칭과 수어 영상 재생에 더 유리한 형태로 바꾸는 단계다. 조사 제거, 시간과 장소 분리, 질문 여부 판단, 수어식 순서 재배치 같은 작업이 포함된다."
    )
    add_h2(doc, "10.7 후보(Candidate)라는 생각")
    add_para(
        doc,
        "자연어 처리에서는 항상 정답 하나만 즉시 얻기 어렵다. 그래서 여러 후보를 만들고 그중 가장 실용적인 결과를 고르는 경우가 많다. 이 프로젝트도 rule, etri, openai 후보를 비교한다."
    )
    add_h2(doc, "10.8 사전 hit")
    add_para(
        doc,
        "사전 hit는 후보 토큰이 실제 수어 사전에 얼마나 잘 맞는지를 보는 생각이다. 이 프로젝트의 최종 목표는 예쁜 언어 분석이 아니라 실제 수어 영상을 재생하는 것이므로, 사전 hit는 매우 실용적인 선택 기준이다."
    )
    add_h2(doc, "10.9 Fallback")
    add_para(
        doc,
        "Fallback은 주 방식이 실패했을 때 대신 사용하는 예비 방식이다. OpenAI나 ETRI가 실패해도 rule 기반 분석으로 계속 진행할 수 있으면 서비스 전체가 멈추지 않는다. 안정적인 서비스일수록 fallback 전략이 중요하다."
    )
    add_h2(doc, "10.10 Unknown과 noVideoWords의 차이")
    add_para(
        doc,
        "unknown은 언어 차원에서 사전에 연결되지 못한 단어를 뜻하고, noVideoWords는 언어 차원에서는 알지만 영상 자산이 없는 단어를 뜻한다. 둘을 나누는 이유는 문제 원인이 다르기 때문이다. 하나는 사전 보강 문제이고, 다른 하나는 자산 보강 문제다."
    )

    add_h1(doc, "11. 실제 요청을 통해 개념을 묶어서 이해하기")
    add_h2(doc, "11.1 번역 요청 예시")
    add_flow(
        doc,
        "번역 요청에서 개념들이 만나는 지점",
        [
            "사용자 입력",
            "  ↓ 프런트엔드 state",
            "번역 버튼 클릭",
            "  ↓ API 요청",
            "HTTP POST /translate",
            "  ↓ Controller",
            "TranslationService",
            "  ↓ 정규화, 토큰화, 후보 선택, Storage URL 조회",
            "JSON 응답",
            "  ↓ 프런트엔드 result state",
            "화면 리렌더링",
        ],
    )
    add_para(
        doc,
        "이 한 번의 요청 안에 지금까지 설명한 개념이 거의 다 들어 있다. 프런트엔드 상태, API, HTTP, JSON, Controller, Service, 정규화, 후보 선택, Storage URL, 리렌더링이 모두 한 줄로 이어진다."
    )
    add_h2(doc, "11.2 로그인 요청 예시")
    add_flow(
        doc,
        "로그인 요청에서 개념들이 만나는 지점",
        [
            "사용자 Google 로그인",
            "  ↓ idToken 획득",
            "프런트엔드가 서버로 전송",
            "  ↓ 서버 검증",
            "GoogleAuthService",
            "  ↓ 사용자 조회",
            "JWT 발급",
            "  ↓ 로컬 저장소에 저장",
            "인증 상태 복원 가능",
        ],
    )
    add_h2(doc, "11.3 퀴즈 요청 예시")
    add_flow(
        doc,
        "퀴즈 요청에서 개념들이 만나는 지점",
        [
            "프런트엔드 quiz 화면 진입",
            "  ↓ GET 세션 API",
            "백엔드가 Firestore 문제 조회",
            "  ↓ SessionQuestion 응답",
            "프런트엔드가 문제 배열 state 저장",
            "사용자 선택지 클릭",
            "  ↓ POST answer API",
            "백엔드가 정답 판정과 통계 갱신",
            "프런트엔드가 결과 화면 갱신",
        ],
    )

    add_h1(doc, "12. 초보자가 자주 헷갈리는 지점")
    add_table(
        doc,
        ["헷갈리는 지점", "왜 헷갈리나", "정리"],
        [
            ("프런트엔드와 백엔드", "둘 다 앱의 일부처럼 보여서", "프런트는 화면, 백엔드는 처리라고 나누면 된다."),
            ("상태와 저장소", "둘 다 값을 기억해서", "상태는 실행 중 메모리, 저장소는 앱을 껐다 켜도 남는 값이다."),
            ("인증과 인가", "둘 다 보안처럼 보여서", "인증은 누구인지 확인, 인가는 무엇을 할 수 있는지 판단이다."),
            ("데이터베이스와 Storage", "둘 다 저장소라서", "DB는 구조화된 값, Storage는 큰 파일이다."),
            ("토큰과 JWT", "이름이 비슷해서", "토큰은 일반 개념, JWT는 특정 형식의 인증 토큰이다."),
            ("unknown과 noVideoWords", "둘 다 결과 부족이라서", "unknown은 사전 미매칭, noVideoWords는 영상 자산 부족이다."),
        ],
        [1.8, 2.0, 2.6],
    )
    add_callout(
        doc,
        "학습 팁",
        "코드를 읽다가 모르는 단어가 나오면 멈추고 개념 문서로 돌아오는 습관이 좋다. 개념을 이해하지 못한 채 코드를 계속 읽으면 단기적으로는 지나갈 수 있어도 전체 구조를 오래 기억하기 어렵다.",
    )

    add_h1(doc, "13. 이 프로젝트에서 꼭 기억하면 좋은 핵심 문장")
    add_numbered_list(
        doc,
        [
            "프런트엔드는 입력과 화면 상태를 담당하고, 백엔드는 규칙과 저장과 검증을 담당한다.",
            "API는 프런트엔드와 백엔드의 계약이다.",
            "JSON은 그 계약을 실제로 주고받는 데이터 형식이다.",
            "React에서는 상태가 바뀌면 화면이 다시 그려진다.",
            "Spring에서는 Controller가 입구이고 Service가 실제 작업자다.",
            "JWT는 서버가 발급한 로그인 상태 표현 토큰이다.",
            "Firestore는 문서형 데이터 저장소이고 Storage는 파일 저장소다.",
            "번역 파이프라인의 목표는 가장 똑똑한 분석이 아니라 가장 재생 가능한 결과를 만드는 것이다.",
            "unknown과 noVideoWords를 구분해야 문제 원인을 올바르게 볼 수 있다.",
            "보안은 화면에서 가리는 것이 아니라 서버에서 강제하는 것이다.",
        ],
    )

    add_h1(doc, "14. 마지막 정리")
    add_para(
        doc,
        "개발이 어렵게 느껴지는 가장 큰 이유는 코드보다 개념이 먼저 정리되지 않기 때문이다. 하지만 개념을 분리해 놓고 보면 앱, 프런트엔드, 백엔드, API, 인증, 저장소, 번역 파이프라인은 각자 분명한 역할을 가진 조각들이다."
    )
    add_para(
        doc,
        "이 문서를 여러 번 볼 때는 모든 내용을 한 번에 외우려 하지 말고, 현재 읽고 있는 코드와 연결해서 보는 것이 좋다. 예를 들어 auth-context를 읽고 있다면 상태와 저장소, 로그인 개념 장을 다시 보고, TranslationService를 읽고 있다면 토큰화와 정규화 장을 다시 보는 식으로 연결하면 학습 속도가 훨씬 빨라진다."
    )

    OUT.parent.mkdir(parents=True, exist_ok=True)
    doc.save(OUT)


if __name__ == "__main__":
    build_doc()
    print(OUT)
